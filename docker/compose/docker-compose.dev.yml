services:
  traefik:
    image: traefik:v2.10
    container_name: traefik
    profiles: ["development"]
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.docker.network=inferoute-net"
      - "--entrypoints.web.address=:80"
      - "--log.level=DEBUG"
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api.rule=HostRegexp(`{host:.+}`)"
      - "traefik.http.routers.api.entrypoints=web"
    networks:
      - inferoute-net
    restart: unless-stopped

  orchestrator:
    build:
      args:
        ENVIRONMENT: development
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.orchestrator.rule=PathPrefix(`/v1`)"
      - "traefik.http.routers.orchestrator.entrypoints=web"
      - "traefik.http.services.orchestrator.loadbalancer.server.port=8080"

  provider-management:
    build:
      args:
        ENVIRONMENT: development
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.provider-management.rule=PathPrefix(`/api/provider`)"
      - "traefik.http.routers.provider-management.entrypoints=web"
      - "traefik.http.services.provider-management.loadbalancer.server.port=8082"

  provider-health:
    build:
      args:
        ENVIRONMENT: development
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.provider-health.rule=PathPrefix(`/api/health/providers`)"
      - "traefik.http.routers.provider-health.entrypoints=web"
      - "traefik.http.services.provider-health.loadbalancer.server.port=8084"

  model-pricing:
    build:
      args:
        ENVIRONMENT: development
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.model-pricing.rule=PathPrefix(`/api/model-pricing`)"
      - "traefik.http.routers.model-pricing.entrypoints=web"
      - "traefik.http.services.model-pricing.loadbalancer.server.port=8085"

  auth:
    build:
      args:
        ENVIRONMENT: development

  provider-communication:
    build:
      args:
        ENVIRONMENT: development

  scheduler:
    build:
      args:
        ENVIRONMENT: development

  payment-processing:
    build:
      args:
        ENVIRONMENT: development
